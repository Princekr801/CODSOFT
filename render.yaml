services:
  - type: web
    name: talenthub-job-board
    env: node
    plan: free
    rootDir: job-board
    buildCommand: npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend
    startCommand: npm start --prefix backend
    envVars:
      - key: NODE_ENV
        value: production
